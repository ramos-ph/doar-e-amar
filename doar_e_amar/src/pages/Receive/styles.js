import {StyleSheet} from 'react-native';

import {common} from '../../styles';

export default StyleSheet.create({
  ...common,

  label: {
    width: '85%',
    textAlign: 'center',
    marginBottom: 30,
    fontSize: 18,
  },
});
